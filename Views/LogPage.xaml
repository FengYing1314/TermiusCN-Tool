<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="TermiusCN_Tool.Views.LogPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 顶部标题和操作栏 -->
        <StackPanel Grid.Row="0" Spacing="16">
            <TextBlock Text="操作日志" Style="{StaticResource TitleTextBlockStyle}"/>

            <CommandBar DefaultLabelPosition="Right" Background="Transparent">
                <AppBarButton Icon="Delete" Label="清空日志"
                             Command="{x:Bind ViewModel.ClearCommand}"/>
                <AppBarButton Icon="Save" Label="导出日志"
                             Command="{x:Bind ViewModel.ExportCommand}"/>
            </CommandBar>
        </StackPanel>

        <!-- 日志显示区域 -->
        <ScrollViewer Grid.Row="1" Margin="0,16,0,0">
            <TextBox Text="{x:Bind ViewModel.LogText, Mode=OneWay}"
                    IsReadOnly="True"
                    TextWrapping="Wrap"
                    AcceptsReturn="True"
                    FontFamily="Consolas"
                    FontSize="12"
                    MinHeight="400"
                    Background="{ThemeResource LayerFillColorDefaultBrush}"/>
        </ScrollViewer>
    </Grid>
</Page>
