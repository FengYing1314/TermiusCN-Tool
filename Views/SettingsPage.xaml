<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="TermiusCN_Tool.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:TermiusCN_Tool.Converters"
    mc:Ignorable="d">

    <ScrollViewer>
        <StackPanel Padding="24" Spacing="24" MaxWidth="900" HorizontalAlignment="Center">

            <TextBlock Text="设置" Style="{StaticResource TitleTextBlockStyle}"/>

            <!-- Termius 路径 -->
            <StackPanel Spacing="8">
                <TextBlock Text="Termius 安装路径" Style="{StaticResource SubtitleTextBlockStyle}"/>
                <Grid ColumnDefinitions="*,Auto" ColumnSpacing="8">
                    <TextBox Grid.Column="0"
                            Text="{x:Bind ViewModel.CustomPath, Mode=TwoWay}"
                            PlaceholderText="留空则自动检测默认路径"/>
                    <Button Grid.Column="1"
                           Content="浏览..."
                           Command="{x:Bind ViewModel.BrowseCommand}"/>
                </Grid>
                <TextBlock Text="默认路径: %LOCALAPPDATA%\Programs\Termius"
                          FontSize="12"
                          Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                <Border Background="{ThemeResource AccentFillColorDefaultBrush}" 
                        CornerRadius="8" 
                        Padding="12"
                        Margin="0,8,0,0">
                    <StackPanel Spacing="4">
                        <TextBlock Text="💡 智能检测提示" 
                                  FontSize="12" 
                                  FontWeight="SemiBold"
                                  Foreground="White"/>
                        <TextBlock TextWrapping="Wrap" 
                                  FontSize="11"
                                  Foreground="White">
                            留空时自动检测顺序：① 默认路径 → ② 运行中的 Termius 进程
                        </TextBlock>
                        <TextBlock TextWrapping="Wrap" 
                                  FontSize="11"
                                  Foreground="White"
                                  Margin="0,4,0,0">
                            💡 如未找到，请先启动 Termius，然后在主页点击「刷新状态」按钮
                        </TextBlock>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- 默认汉化类型 -->
            <StackPanel Spacing="8">
                <TextBlock Text="默认汉化类型" Style="{StaticResource SubtitleTextBlockStyle}"/>
                <ComboBox SelectedIndex="{x:Bind ViewModel.DefaultTypeIndex, Mode=TwoWay}"
                         HorizontalAlignment="Stretch">
                    <ComboBoxItem Content="标准版 (仅汉化)"/>
                    <ComboBoxItem Content="试用版 (推荐)"/>
                    <ComboBoxItem Content="离线版 (跳过登录)"/>
                </ComboBox>
                <TextBlock Text="设置启动时默认选中的汉化版本类型"
                          FontSize="12"
                          Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            </StackPanel>

            <!-- HTTP 代理 -->
            <StackPanel Spacing="8">
                <TextBlock Text="HTTP 代理 (可选)" Style="{StaticResource SubtitleTextBlockStyle}"/>
                <TextBox Header="代理地址"
                        Text="{x:Bind ViewModel.ProxyAddress, Mode=TwoWay}"
                        PlaceholderText="例如: 127.0.0.1"/>
                <NumberBox Header="代理端口"
                          Value="{x:Bind ViewModel.ProxyPort, Mode=TwoWay}"
                          PlaceholderText="例如: 7890"
                          Minimum="0"
                          Maximum="65535"
                          SpinButtonPlacementMode="Compact"/>
                <Button Command="{x:Bind ViewModel.TestProxyCommand}"
                       IsEnabled="{x:Bind ViewModel.IsTesting, Mode=OneWay, Converter={StaticResource InverseBoolConverter}}">
                    <Button.Content>
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <ProgressRing Width="16" Height="16"
                                        IsActive="{x:Bind ViewModel.IsTesting, Mode=OneWay}"
                                        Visibility="{x:Bind ViewModel.IsTesting, Mode=OneWay}"/>
                            <TextBlock Text="测试连接"/>
                        </StackPanel>
                    </Button.Content>
                </Button>
                <TextBlock Text="配置代理可解决无法访问 GitHub 的问题"
                          FontSize="12"
                          Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            </StackPanel>

            <!-- 其他选项 -->
            <StackPanel Spacing="16">
                <TextBlock Text="其他选项" Style="{StaticResource SubtitleTextBlockStyle}"/>

                <ToggleSwitch Header="启动时检查更新"
                             IsOn="{x:Bind ViewModel.AutoCheckUpdate, Mode=TwoWay}"
                             OnContent="开启"
                             OffContent="关闭"/>

                <ToggleSwitch Header="显示警告对话框"
                             IsOn="{x:Bind ViewModel.ShowWarnings, Mode=TwoWay}"
                             OnContent="开启"
                             OffContent="关闭"/>
            </StackPanel>

            <!-- 操作按钮 -->
            <StackPanel Spacing="12">
                <Button Content="保存设置"
                       Style="{StaticResource AccentButtonStyle}"
                       Command="{x:Bind ViewModel.SaveCommand}"
                       HorizontalAlignment="Stretch"
                       Height="40"/>

                <Button Content="恢复默认设置"
                       Command="{x:Bind ViewModel.ResetToDefaultCommand}"
                       HorizontalAlignment="Stretch"/>
            </StackPanel>

            <!-- 关于信息 -->
            <StackPanel Spacing="8" Padding="16" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                       CornerRadius="8">
                <TextBlock Text="关于" Style="{StaticResource SubtitleTextBlockStyle}"/>
                <TextBlock TextWrapping="Wrap">
                    <Run Text="Termius 汉化工具 v1.0"/>
                    <LineBreak/>
                    <Run Text="基于 .NET 10 + WinUI 3 开发" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <LineBreak/>
                    <LineBreak/>
                    <Run Text="汉化文件来源: " FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <Hyperlink NavigateUri="https://github.com/ArcSurge/Termius-Pro-zh_CN">
                        <Run Text="ArcSurge/Termius-Pro-zh_CN"/>
                    </Hyperlink>
                </TextBlock>
            </StackPanel>

        </StackPanel>
    </ScrollViewer>

    <Page.Resources>
        <converters:InverseBoolConverter x:Key="InverseBoolConverter"/>
    </Page.Resources>
</Page>
